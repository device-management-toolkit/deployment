name: Build Windows Installer

on:
  workflow_dispatch:
    inputs:
      console_version:
        description: "Console release tag to package. Empty = latest."
        required: false
        default: ""

permissions:
  contents: read

jobs:
  build:
    name: Build Windows installer (stub)
    runs-on: windows-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      - name: Not yet implemented
        shell: bash
        run: |
          echo "::warning::Windows installer is not yet implemented."
          echo "Add scripts under installers/windows/ and replace this stub."
          echo "See installers/macos/ for reference layout (build script + payload scripts + resources)."
          exit 0
